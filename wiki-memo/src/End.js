export function End({handleClick}) {
    return (
        <>
            <h1>The End Page</h1>
            <button className="btn btn-primary mt-4" onClick={handleClick}>Play it again, Sam!</button>
        </>
    )
}